class FridgeUser {
  final String email;
  final String first_name;
  final String last_name;
  final String phone_number;
  final DateTime created_at;
  final DateTime updated_at;

  FridgeUser(this.email, this.first_name, this.last_name, this.phone_number, this.created_at, this.updated_at);
}
